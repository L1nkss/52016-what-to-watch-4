const AuthorizationStatus = {
  AUTH: `AUTH`,
  NO_AUTH: `NO_AUTH`,
};

const ActionType = {
  REQUIRED_AUTHORIZATION: `REQUIRED_AUTHORIZATION`,
  REQUIRED_AUTHORIZATION_DATA: `REQUIRED_AUTHORIZATION_DATA`
};

export {AuthorizationStatus, ActionType};
